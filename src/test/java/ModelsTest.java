import model.WeatherPresentation;
import model.WeatherUpdateClass;
import org.powermock.core.classloader.annotations.PrepareOnlyThisForTest;

@PrepareOnlyThisForTest({WeatherUpdateClass.class, WeatherPresentation.class})
public class ModelsTest {
   private WeatherPresentation weatherPresentation;
   private WeatherUpdateClass weatherUpdateClass;
   // TODO: create mocks for the two classes and see if they match


}
